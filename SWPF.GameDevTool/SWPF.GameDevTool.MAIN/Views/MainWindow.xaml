<base:WindowBase x:Class="SWPF.GameDevTool.MAIN.Views.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:base="clr-namespace:SWPF.Common.Base;assembly=SWPF.Common"
             xmlns:cm="clr-namespace:SWPF.Common.Converters;assembly=SWPF.Common"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SWPF.GameDevTool.MAIN.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <base:WindowBase.Resources>
        <cm:BoolToVisibilityConverter x:Key="BoolToVisConverter" CollapseWhenFalse="True" />
    </base:WindowBase.Resources>
    <Grid x:Name="gdMain">


        <StackPanel Margin="20">
            <CheckBox Content="Show Text" />
            <TextBlock Text="Hello World!"
                   Visibility="{Binding IsTextVisible, Converter={StaticResource BoolToVisConverter}}"
                   FontSize="24" />
        </StackPanel>
        
        <Grid x:Name="gdLoginPane" Visibility="Collapsed" >
            <local:LoginPanel x:Name="ucLogin" />
        </Grid>
        <Grid x:Name="gdTaskPane" Visibility="Visible">
            <local:LobbyPanel x:Name="pnlIcons" VerticalAlignment="Top" Margin="0" Width="400" Height="320"  AppBtnClick="appBtn_Click"/>
        </Grid>
    </Grid>
</base:WindowBase>
